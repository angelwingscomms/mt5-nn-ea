let it use focal loss
implement nn.py flag -c to use castor architecture instead
for any architecture, be it -m or -c, add flag -a to use multihead attention in the model
make sure sl tp multipliers are 5.4 both for label and default
add -d flag to test.py to test just on a single day, if -d flag is present, optional date arg in form ddmmyy else defaults to previous day
let test run folders add ' d' to the end of the folder name, after the date, to indicate that it is a daily, not monthly, test run
train and test model on yesterday to make sure model training and testing works completely well with the new flags and changes
remove all logic for creating last_model folders, so live.mq5 uses a model version number directly instead of 'last_model' to load the model, and make sure that the last model version number is updated correctly after each training run
figure out why the last BTC model seems to keep choosing 'HOLD' in tests and search the net for cheap, fast, low memory ways to fix how the last BTC model seems to keep choosing hold in tests